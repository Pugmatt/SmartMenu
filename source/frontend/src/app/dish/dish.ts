import { Restaurant } from "../restaurant";

export class Dish {
    id: string;
    name: string;
    description: string;
    category: string;
    restaurant: string;
    images: string[];
    rating: number;
}
